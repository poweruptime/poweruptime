{
  "name": "poweruptime",
  "version": "0.0.1",
  "scripts": {
    "web:build": "pnpm -C web build --configuration development",
    "web:build:prod": "pnpm -C web build --configuration production",
    "web:start": "pnpm -C web start",
    "web:start:ssr": "pnpm -C web start:ssr",
    "web:api": "pnpm -C web api",
    "emails:start": "pnpm -C emails start",
    "emails:export": "pnpm -C emails export",
    "format:check": "prettier --check .",
    "detekt": "./gradlew detekt && prettier --write .",
    "format": "prettier --write ."
  },
  "private": true,
  "devDependencies": {
    "@trivago/prettier-plugin-sort-imports": "^5.2.2",
    "prettier": "3.5.1",
    "prettier-plugin-organize-attributes": "1.0.0",
    "prettier-plugin-sort-json": "^4.1.1",
    "prettier-plugin-tailwindcss": "0.6.11"
  }
}
