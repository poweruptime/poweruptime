project_name: pu
version: 2

builds:
  - env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64

archives:
  - formats: binary

checksum:
  name_template: 'checksums.txt'

release:
  prerelease: auto
